export const defaultConfig = {
  tabWidth: 2,
  dayStartsAt: {hours: 12, minutes:0}, // not a sensible default
  defaultEventDuration: {hours: 1, minutes: 0},
  useDescriptionAsLocation: true, // not a sensible default
}

export default defaultConfig;
